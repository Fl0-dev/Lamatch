{% extends 'base.html.twig' %}

{% block title %}{{parent()}}|Ajouter Formation{% endblock %}

{% block body %}
<section class="col-6 align-items-center my-3 mx-auto">
    <h1>Ajouter une formation <a href="{{ path('candidat_modifier',{id:candidat.id}) }}" class="btn btn-secondary">Retour</a></h1>
    {{ form_start(formFormation) }}
    {{ form_widget(formFormation) }}
    <button type="submit" class="btn btn-primary">Inscrire</button>
    {{ form_end(formFormation) }}


</section>

{% endblock %}
