{% extends 'base.html.twig' %}

{% block title %}{{parent()}}|Nouveau Password{% endblock %}

{% block body %}
    {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <h1>Réinitialiser votre password</h1>

    {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}
        <div>
            <small>
                Inscrivez votre adresse mail et nous vous enverrons un lien pour vous permettre de rinitialiser votre password
            </small>
        </div>

        <button class="btn btn-primary">Envoyer Email </button>
    {{ form_end(requestForm) }}
{% endblock %}
